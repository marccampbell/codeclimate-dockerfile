var expect = require('chai').expect
var dockerfileLint = require('../lib/dockerfileLint.js')

describe("dockerfileLint", function(){
  describe("#runEngine()", function(){
    xit("checks for /config.json", function(){
      // expect();
    });
  });
  describe("#find(file)", function(){
    xit("finds optimizations in a Dockerfile", function(){
      // expect();
    });
  });
});
